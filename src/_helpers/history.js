// Custom history object used by React Router
// This enables redirecting users from outside react components
import { createBrowserHistory } from 'history';

export const history = createBrowserHistory();